# 🧭 项目协作操作规范文档（EBU6304_Group58）

## 👥 团队仓库信息

- GitHub 主仓库地址（由 Keeper0824 管理）：

  ```
  https://github.com/Keeper0824/EBU6304_Group58.git
  ```

- 所有成员通过协作链接（Collaborator）加入该仓库，无需 fork。

------

## ✅ 分支策略说明

| 分支名             | 用途                                                        |
| ------------------ | ----------------------------------------------------------- |
| `main`             | 主分支，仅用于最终提交或演示版本（不直接开发）              |
| `dev`              | 开发整合分支，合并所有功能开发分支                          |
| `feature/xxx-name` | 每个人开发一个功能时使用的分支，例如：`feature/login-alice` |

------

## 🚀 开发流程规范

### 1️⃣ 克隆仓库（首次使用）

```bash
git clone git@github.com:Keeper0824/EBU6304_Group58.git
cd EBU6304_Group58
```

如果使用 HTTPS：

```bash
git clone https://github.com/Keeper0824/EBU6304_Group58.git
```

------

### 2️⃣ 创建个人功能分支

先拉取 `dev` 最新代码：

```bash
git checkout dev
git pull origin dev
```

然后创建功能分支（命名规范 `feature/功能名-名字`）：

```bash
git checkout -b feature/功能名-你的名字
```

示例：

```bash
git checkout -b feature/register-bob
```

------

### 3️⃣ 开发 + 提交 + 推送

开发后提交代码：

```bash
git add .
git commit -m "完成注册页面UI"
```

第一次推送时建立跟踪关系：

```bash
git push --set-upstream origin feature/注册功能-bob
```

以后可直接用：

```bash
git push
```

------

### 4️⃣ 发起 Pull Request（合并到 `dev`）（不要做这步）

- 推送后打开 GitHub 页面，会自动提示创建 Pull Request

- 选择：

  ```
  base: dev  ←  compare: feature/xxx-name
  ```

- 填写标题 + 描述（做了哪些功能）

- 点击 `Create Pull Request`

- 等待另一个组员 code review 后点击 **Merge**

------

### 5️⃣ 保持代码同步（开发中每隔一两天做一次）

```bash
git checkout dev
git pull origin dev

git checkout feature/你的分支名
git merge dev
```

如果遇到冲突，IDEA 或 Git 会提示你手动解决。

------

### 6️⃣ 最终整合 & 演示（不要做这步）

- 所有人功能合并进 `dev` 后
- 测试无误，再合并到 `main`：

```bash
git checkout main
git pull origin main
git merge dev
git push origin main
```

------

## 🔒 提交规范（可选）

```bash
feat: 添加用户注册功能
fix: 修复密码验证逻辑错误
docs: 更新使用说明
refactor: 重构登录页面逻辑
```

------

## 📌 注意事项

- **不要直接在 `main` 上开发或提交**
- 每个功能一个分支，不要多人修改同一分支
- 每次开发前 `git pull origin dev` 保证更新
- 合并到 `dev` 要通过 Pull Request，不直接 push
- 建议每次合并前自己先 `merge dev` 测试无误再发 PR

------

好，既然你想用 **SSH 模式** 来团队协作，我会一步一步带你和队友都顺利配置好！💪

------





## ✅ 一次性配置 SSH 的完整流程------使用不会卡顿的PUSH/PULL（每个队员都要做）

------

### 🔧 第 1 步：检查是否已经有 SSH Key

在终端中输入（PowerShell、CMD、Git Bash 或 IDEA Terminal 都可以）：

```bash
ls ~/.ssh
```

看看有没有：

```
id_ed25519 和 id_ed25519.pub
```

如果没有，进入下一步。

------

### 🧪 第 2 步：生成 SSH key（推荐 Ed25519 类型）

```bash
ssh-keygen -t ed25519 -C "你的GitHub邮箱"
```

连续回车就行，不要输入密码（除非你想设置加密）

生成的文件默认在：

```
C:\Users\你的用户名\.ssh\
```

------

### 🔐 第 3 步：添加 SSH 公钥到 GitHub

1. 复制公钥内容：

```bash
cat ~/.ssh/id_ed25519.pub
```

1. 打开 [GitHub SSH Keys 页面](https://github.com/settings/keys)
2. 点击右上角 **“New SSH key”**：
   - Title：随便填，比如 `MyLaptop`、`IDEA`、`MyPC`
   - Key：粘贴刚刚复制的 `.pub` 内容
   - Save

------

### 🚀 第 4 步：测试是否配置成功

```bash
ssh -T git@github.com
```

第一次连接会问你是否信任，输入 `yes`
 如果看到这句话说明成功了：

```
Hi Keeper0824! You've successfully authenticated, but GitHub does not provide shell access.
```

------

### 🔁 第 5 步：将远程地址设置为 SSH（一次即可）

```bash
git remote set-url origin git@github.com:Keeper0824/EBU6304_Group58.git
```

验证一下：

```bash
git remote -v
```

你应该看到：

```
origin  git@github.com:Keeper0824/EBU6304_Group58.git (fetch)
origin  git@github.com:Keeper0824/EBU6304_Group58.git (push)
```

------

### ✅ 现在你可以愉快使用 SSH 了：

```bash
git pull
git push
```

不用再输用户名密码！

------

### ❓其他同学怎么办？

他们也照这个流程做就可以，每个人生成并上传自己的 key，GitHub 会识别是谁。

------

需要我给你生成一个 `.md` 文件版本，发给队友也能照着配吗？或者你想我现在帮你验证 SSH 能不能用了？